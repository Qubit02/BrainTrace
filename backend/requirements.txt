# =============================================================================
# BrainTrace Backend Requirements
# =============================================================================

# 웹 서버 및 API 프레임워크
fastapi==0.104.1          # FastAPI 웹 프레임워크
uvicorn==0.24.0           # ASGI 서버

# 데이터베이스
neo4j==5.14.1             # 그래프 데이터베이스

# LLM 및 AI 처리
langchain==0.2.5          # LLM 프레임워크
openai==1.78.1            # OpenAI API 클라이언트
sentence-transformers>=2.3.1  # 문장 임베딩
transformers>=4.40.0      # Hugging Face 모델
huggingface_hub>=0.30.0   # Hugging Face Hub

# 벡터 검색
qdrant-client==1.9.0      # 벡터 데이터베이스 클라이언트

# 오디오 처리
librosa==0.10.1
soundfile==0.12.1
torch==2.7.1

# 기타 필수 의존성
numpy>=1.24.0
scipy>=1.10.0
tqdm
ollama  
dotenv
python-multipart
markdown>=3.5.0
python-docx

# libraries for embedding
gensim
konlpy